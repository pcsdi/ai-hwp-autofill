# 한글문서 자동작성 Windows 앱 1

## 실행 방법
1. 이 폴더의 `한글문서자동작성_실행.bat`를 더블클릭합니다.
2. 처음 한 번만 `hwp.exe` 위치를 선택합니다.
3. HWP/HWPX 문서를 선택합니다.
4. AI와 나눈 대화내용을 붙여넣습니다.
5. `문서 분석 + 자동 매칭`을 누릅니다.
6. 자동으로 찾은 값과 `확인 필요` 항목을 확인합니다.

## 중요한 현재 범위
이 1차 Windows 앱은 **Python 설치가 필요 없습니다.**
이미 테스트한 `hwp.exe`를 뒤에서 호출합니다.

다만 임의의 HWP 문서에서 '빈 표 셀의 정확한 좌표'를 100% 자동으로 확정하는 부분은 아직 개발 중입니다.
그래서 1차 버전은:
- HWP/HWPX 읽기
- 문서 항목 감지
- 대화에서 날짜/대상/인원/프로그램명 등의 값 추출
- 자동 매칭 제안
- 기존값=>새값의 안전한 정확 교체
- 원본을 건드리지 않고 새 HWP/HWPX 저장

까지 제공합니다.

최종 목표는 `정확 교체` 칸도 없애고,
문서의 빈 표 셀과 사진 위치를 자동 식별하여 바로 작성하는 것입니다.
